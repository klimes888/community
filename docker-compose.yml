version: "3"
services:
  #   node:
  #     build: .
  #     ports:
  #      - "8080:8080"
  #     expose:
  #       - "8080"
  #     links:
  #       - mysql
  #     depends_on:
  #       - mysql
  mysql:
    image: "mysql:latest"
    ports:
      - "3306:3306"
    volumes:
      - ../../mysql_data:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: "1234"
      MYSQL_DATABASE: "cm"
      MYSQL_USER: "taehee"
      MYSQL_PASSWORD: "1234"
    expose:
      - "3306"
